{
  "$schema": "http://json-schema.org/schema#",
  "title": "Monitoring Data",
  "type": "object",
  "properties": {
    "check": {
      "type": "string"
    },
    "downtime": {
      "description": "Each document describes an hour so the maximum number of seconds is 3600.",
      "type": "integer",
      "minimum": 0,
      "maximum": 3600
    },
    "unmonitored": {
      "description": "Each document describes an hour so the maximum number of seconds is 3600.",
      "type": "integer",
      "minimum": 0,
      "maximum": 3600
    },
    "uptime": {
      "description": "Each document describes an hour so the maximum number of seconds is 3600.",
      "type": "integer",
      "minimum": 0,
      "maximum": 3600
    }
  },
  "required": ["check", "downtime", "unmonitored", "uptime"],
  "additionalFields": false
}

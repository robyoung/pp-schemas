{
  "title": "Monitoring Data",
  "type": "object",
  "properties": {
    "_timestamp": {
      "description": "An RFC3339 formatted date time, the API actually requies ISO8601 but the differences are minimal. RFC3339 is more explicit, maybe we should switch?",
      "type": "string",
      "format": "date-time"
    },
    "check": {
      "type": "string"
    },
    "downtime": {
      "description": "Each document describes an hour so the maximum number of seconds is 3600.",
      "type": "integer",
      "minimum": 0,
      "maximum": 3600
    },
    "unmonitored": {
      "description": "Each document describes an hour so the maximum number of seconds is 3600.",
      "type": "integer",
      "minimum": 0,
      "maximum": 3600
    },
    "uptime": {
      "description": "Each document describes an hour so the maximum number of seconds is 3600.",
      "type": "integer",
      "minimum": 0,
      "maximum": 3600
    }
  },
  "required": ["_timestamp", "check", "downtime", "unmonitored", "uptime"]
}
